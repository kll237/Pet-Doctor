node_modules
dist
dist-ssr
*.local
.vscode
.idea
.DS_Store
*.log
.vite
coverage

# vite temp
vite.config.ts.timestamp-*.mjs

# vite temp
vite.config.ts.timestamp-*.mjs

# verification artifacts
screenshots/
*-dom.html
screenshot_verify.mjs
screenshot_home.mjs
screenshot_log.mjs
screenshot_cats.mjs

# python cutout helpers (throwaway)
remove_white_bg*.py

# vite log/error temp files (locked while server runs)
vite.log
vite.err
*.err

# stale build rename (vite build workaround)
dist_bak/

# local dev-server launcher (keep locally, don't publish)
_launch-vite.ps1

# 社区后端运行时数据（本地多用户互通，含测试数据）
server/data.json
